# وابستگی‌های اضافه فقط برای ماژول video_gen
# روی سرور اصلی نصب نکن - این‌ها سنگین‌اند (چند گیگ)
# فقط روی Colab / GPU node نصب کن:
#   pip install -r app/video_gen/requirements-video.txt

diffusers>=0.32.0
transformers>=4.46.0
accelerate>=1.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
sentencepiece>=0.2.0
protobuf>=4.25.0
Pillow>=10.0.0
torch>=2.4.0
torchvision>=0.19.0
httpx>=0.28.0

# اختیاری برای بعضی مدل‌ها
# einops
# omegaconf
# safetensors
